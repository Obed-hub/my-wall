rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Files are stored in uploads/{userId}/{fileName}
    match /uploads/{userId}/{allPaths=**} {
      // Only allow access if the user is authenticated and matches the folder userId
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}